
DATABASE_URL=
JWT_SECRET=
UPLOADTHING_TOKEN=
OPEN_WEATHER_API_KEY=

# Secret for cron endpoints (e.g. cleanup-uploadthing). Generate a random string.
CRON_SECRET=

# Public website URL for CORS (e.g. https://yoursite.com)
PUBLIC_WEBSITE_URL=
SMTP_APP_PASSWORD=

# Email notifications (outbound SMTP). All required to enable email dispatch.
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="Wahd Omrania Notifications <notifications@wahdomrania.sa>"
# Public app URL used for CTA links inside email bodies (login, deep links).
APP_URL=http://localhost:3000
